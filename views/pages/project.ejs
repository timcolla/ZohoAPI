<!DOCTYPE html>
<html lang="en">
<head>
	<% include ../partials/head %>
</head>
<body class="container">

	<header>
		<% include ../partials/header %>
	</header>

	<main>
		<div class="row">
			<div class="col-sm-12">
			
  			<div class="jumbotron">
          <% if (locals.projectName) { %>
              <h2><%= projectName %></h2>
            <% } else { %>
              <h2>Error</h2>
            <% } %>
          <p>
            <% if (locals.error) { %>
              <p>Error: <%= error %></p>
            <% } %>
            <% if (locals.jobs) { %>
              <ul>
              <%for (var index in jobs){
              var job = jobs[index];
              %>
                <li><%=job["jobName"]%> (ID: <%=job["jobId"]%>)</li>    
                <%}%>
              </ul>
            <% } %>
          </p>
  			</div>
				
			</div>
		</div>
	</main>

	<footer>
		<% include ../partials/footer %>
	</footer>
	
</body>
</html>